import styled from 'styled-components'

export const Container = styled.div`
  margin: 0 20px;

  display: flex;
  justify-content: center;
  align-items: center;
  flex: 1;

  img {
    height: 600px;
    object-fit: cover;
  }
`